# AI core / inference
onnxruntime-gpu[cuda,cudnn]     
scipy
librosa
opencv-python

# Backend (FastAPI)
fastapi[standard]
sqlalchemy
alembic
asyncpg
bcrypt
python-jose[cryptography]
boto3

# Test (tùy chọn, có thể bỏ khi deploy)
pytest
pytest-asyncio
